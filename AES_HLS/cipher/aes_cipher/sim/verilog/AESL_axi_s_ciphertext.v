// ==============================================================
// File generated by Vivado(TM) HLS - High-Level Synthesis from C, C++ and SystemC
// Version: 2017.4
// Copyright (C) 1986-2017 Xilinx, Inc. All Rights Reserved.
// 
// ==============================================================


`timescale 1 ns / 1 ps

`define TV_OUT_ciphertext_TDATA "../tv/rtldatafile/rtl.AES_Encrypt.autotvout_ciphertext.dat"

`define AUTOTB_TRANSACTION_NUM 1

module AESL_axi_s_ciphertext (
    input clk,
    input reset,
    input [8 - 1:0] TRAN_ciphertext_TDATA,
    input TRAN_ciphertext_TVALID,
    output TRAN_ciphertext_TREADY,
    input ready,
    input done,
    output [31:0] transaction);

    wire TRAN_ciphertext_TVALID_temp;
    wire ciphertext_TDATA_full;
    wire ciphertext_TDATA_empty;
    reg ciphertext_TDATA_write_en;
    reg [8 - 1:0] ciphertext_TDATA_write_data;
    reg ciphertext_TDATA_read_en;
    wire [8 - 1:0] ciphertext_TDATA_read_data;
    
    fifo #(16, 8) fifo_ciphertext_TDATA (
        .reset(1'b0),
        .write_clock(clk),
        .write_en(ciphertext_TDATA_write_en),
        .write_data(ciphertext_TDATA_write_data),
        .read_clock(clk),
        .read_en(ciphertext_TDATA_read_en),
        .read_data(ciphertext_TDATA_read_data),
        .full(ciphertext_TDATA_full),
        .empty(ciphertext_TDATA_empty));
    
    always @ (*) begin
        ciphertext_TDATA_write_en <= TRAN_ciphertext_TVALID;
        ciphertext_TDATA_write_data <= TRAN_ciphertext_TDATA;
        ciphertext_TDATA_read_en <= 0;
    end
    assign TRAN_ciphertext_TVALID = TRAN_ciphertext_TVALID_temp;

    
    assign TRAN_ciphertext_TREADY = ~(ciphertext_TDATA_full);
    
    function is_blank_char(input [7:0] in_char);
        if (in_char == " " || in_char == "\011" || in_char == "\012" || in_char == "\015") begin
            is_blank_char = 1;
        end else begin
            is_blank_char = 0;
        end
    endfunction
    
    function [167:0] read_token(input integer fp);
        integer ret;
        begin
            read_token = "";
                    ret = 0;
                    ret = $fscanf(fp,"%s",read_token);
        end
    endfunction
    
    function [167:0] rm_0x(input [167:0] token);
        reg [167:0] token_tmp;
        integer i;
        begin
            token_tmp = "";
            for (i = 0; token[15:0] != "0x"; token = token >> 8) begin
                token_tmp = (token[7:0] << (8 * i)) | token_tmp;
                i = i + 1;
            end
            rm_0x = token_tmp;
        end
    endfunction
    
    reg done_1;
    
    always @ (posedge clk or reset) begin
        if (~reset) begin
            done_1 <= 0;
        end else begin
            done_1 <= done;
        end
    end
    
    reg [31:0] transaction_save_ciphertext_TDATA;
    
    assign transaction = transaction_save_ciphertext_TDATA;
    
    initial begin : AXI_stream_receiver_ciphertext_TDATA
        integer fp;
        reg [8 - 1:0] data;
        reg [8 * 5:1] str;
        
        transaction_save_ciphertext_TDATA = 0;
        fifo_ciphertext_TDATA.clear();
        wait (reset === 1);
        forever begin
            @ (negedge clk);
            if (done_1 == 1) begin
                fp = $fopen(`TV_OUT_ciphertext_TDATA, "a");
                if (fp == 0) begin // Failed to open file
                    $display("ERROR: Failed to open file \"%s\"!", `TV_OUT_ciphertext_TDATA);
                    $finish;
                end
                $fdisplay(fp, "[[transaction]] %d", transaction_save_ciphertext_TDATA);
                while (~fifo_ciphertext_TDATA.empty) begin
                    fifo_ciphertext_TDATA.pop(data);
                    $fdisplay(fp, "0x%x", data);
                end
                $fdisplay(fp, "[[/transaction]]");
                transaction_save_ciphertext_TDATA = transaction_save_ciphertext_TDATA + 1;
                fifo_ciphertext_TDATA.clear();
                $fclose(fp);
            end
        end
    end

endmodule
